# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow admin/private pages (if any)
# ...but link-preview bots (Facebook, WhatsApp, X) must be able to load the share images under /api/og/.
# The more specific Allow wins over Disallow: /api/ (and comes first for parsers that take the first match).
Allow: /api/og/
Disallow: /api/
Disallow: /admin/

# Novel wikis (/novel/:id/wikipedia) are open to crawlers: the SEO worker serves them as real pages, returns 404 for
# drafts and deleted novels, and marks entries without real content noindex.

# Sitemap location
Sitemap: https://www.sardnovels.com/sitemap.xml

# Crawl-delay for good bots (optional)
Crawl-delay: 1
